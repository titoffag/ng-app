import { OrderListByPipe } from './order-list-by.pipe';

describe('OrderListByPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListByPipe();
    expect(pipe).toBeTruthy();
  });
});
